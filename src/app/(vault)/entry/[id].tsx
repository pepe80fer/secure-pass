import { PlaceholderScreen } from '@/ui/components/PlaceholderScreen';

export default function EntryDetail() {
  return <PlaceholderScreen title="Detalle de entrada" phase="Fase 3 — CRUD del vault" />;
}
